// import "prismjs"; // Temporarily commented out to test

export default {
  config: {
    // Add any custom admin configuration here if needed
    locales: ['en'],
  },
  bootstrap(app) {
    // Add any custom bootstrap logic here if needed
    console.log('Strapi admin panel bootstrapped');
  },
};