{
  "name": "strapi-webhook-handler",
  "version": "1.0.0",
  "description": "Webhook handler for Strapi content changes to trigger site rebuilds",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
